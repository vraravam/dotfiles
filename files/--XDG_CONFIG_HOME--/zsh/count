#!/usr/bin/env zsh

# vim:filetype=zsh syntax=zsh tabstop=2 shiftwidth=2 softtabstop=2 expandtab autoindent fileencoding=utf-8

# git command: count — prints the total number of commits on the current branch

# Re-source guard is inside .shellrc itself — safe to call unconditionally.
source "${HOME}/.shellrc"

_count() {
  local folder
  local -a switches
  parse_folder_and_switches "$@"
  section_header2 "$(yellow 'Counting commits') $(purple "${folder}")"
  if ! is_git_repo "${folder}"; then
    warn "Skipping finding count of the repo since '$(yellow "${folder}")' doesn't exist or is not a git repo"
  else
    git -C "${folder}" count "${switches[@]}" || true
  fi
}

count() { dispatch_or_fallback count _count "$@"; }

# Run only when executed directly, not when sourced (e.g. to import the function into another script).
[[ "${zsh_eval_context}" == *file* ]] || count "$@"
# is_zsh returns false in bash (ZSH_VERSION unset), short-circuiting the zsh-only
# '(( $+functions[...] ))' syntax so it is never evaluated by non-zsh runtimes.
is_zsh && (($+functions[compdef])) && compdef count || true
